package com.linrol.cn.tool.utils;

import com.intellij.openapi.project.Project;
import com.intellij.openapi.vcs.VcsNotifier;

import static git4idea.GitNotificationIdsHolder.MERGE_ERROR;

/**
 * Notification utils
 *
 * @author ppolivka
 * @since 5.11.2015
 */
public class MessageUtil {

//    public static void showErrorDialog(Project project, String message, String title) {
//        VcsNotifier.getInstance(project).notifyError(MERGE_ERROR, title, message);
//    }
//
//    public static void showWarningDialog(Project project, String message, String title) {
//        VcsNotifier.getInstance(project).notifyWarning(MERGE_ERROR, title, message);
//    }
//
//    public static void showInfoMessage(Project project, String message, String title) {
//        VcsNotifier.getInstance(project).notifyInfo(MERGE_ERROR, title, message);
//    }
//
//    public static VcsNotifier getNotifier(Project project) {
//        return VcsNotifier.getInstance(project);
//    }

}